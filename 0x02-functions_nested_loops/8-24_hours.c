#include "main.h"

/**
 * jack_bauer -prints every minute of the day of him
 *
 * Return: ...
 */
void jack_bauer(void)
{
	int m,n,b,v;

	for (m=0; m <= 2; m++)
	{
		for (n = 0; n <= 9; n++)
		{
			if ((m <= 1 && n <=9 || (m <= 2 && n <=3))
			{
				for (b = 0; b <= 5; b++)
				{
					for (v = 0; v <= 9; d++)
					{
				        	_putchar(m + '0';
						_putchar(n + '0';
						_putchar(58);
						_putchar(b + '0');
						_putchar(v + '0');
						_putchar('\n');
					}
				}
			}
		}
	}
}


